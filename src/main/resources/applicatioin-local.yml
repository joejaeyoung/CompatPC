spring:
  config:
    activate:
      on-profile: local

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/intelligence?serverTimezone=UTC&characterEncoding=UTF-8&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: jajo

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

## encoding

server:
  servlet:
    encoding:
      force: true
      charset: UTF-8
